# MNS CLI Agent Skill

Agent skill for MNS CLI tool.

## Quick Start
```bash
npx @never-sleeps/cli-darwin-arm64 portfolio
npx @never-sleeps/cli-darwin-arm64 report
node scripts/run-mns.js portfolio
```
